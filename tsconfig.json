{
  "compilerOptions": {
    "target": "es2020",
    "module": "commonjs",
    "rootDir": "./", // Ensures the entire project root is treated as rootDir
    "outDir": "./dist", // (Optional) Specifies where compiled files go
    "esModuleInterop": true,
    "strict": true
  },
  "include": ["**/*"] // Matches all files in the project directory
}

